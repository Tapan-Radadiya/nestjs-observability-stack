services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9000:9090"
    volumes:
      - ./prometheus:/etc/prometheus
      - prometheus_data:/prometheus
  
  grafana:
    image: grafana/grafana-enterprise
    container_name: grafana
    ports:
      - "5000:3000"
    volumes:
      - grafana-loki:/var/lib/grafana
  
volumes:
  prometheus_data:
  grafana-loki: